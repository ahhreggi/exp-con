import "./index.scss";
import Button from "../../Button";

const Hero = () => {
  return (
    <div className="Hero">
      Hero component
      <Button text="TRY IT NOW" styles="bg-rainbow" />
    </div>
  );
};

export default Hero;
