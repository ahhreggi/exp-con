import PropTypes from "prop-types";
import "./Footer.scss";

const Footer = () => {

  Footer.propTypes = {
    setView: PropTypes.func
  };

  return (
    <div className="Footer">

      <div className="contact">

      </div>

    </div>
  );
};

export default Get;
